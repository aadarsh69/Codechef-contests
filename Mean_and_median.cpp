#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, m;
        cin >> n >> m;
        cout << (3 * n - 2 * m, m) << " " << m << " " << max(3 * n - 2 * m, m) << endl;
    }
    return 0;
}