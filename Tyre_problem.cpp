#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int nn, mm;
        cin >> nn >> mm;
        cout << 2 * nn + 4 * mm << endl;
    }
    return 0;
}