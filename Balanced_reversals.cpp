#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int nnu;
        cin >> nnu;
        string bs;
        cin >> bs;
        sort(bs.begin(), bs.end());
        cout << bs << endl;
    }
    return 0;
}