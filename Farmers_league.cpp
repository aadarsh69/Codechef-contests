#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int nb;
        cin >> nb;
        cout << (nb / 2) * 3 << endl;
    }
    return 0;
}