#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int f;
        cin >> f;
        int s[f];
        for (int g = 0; g < f; g++)
        {
            cin >> s[g];
        }
        
    }
    return 0;
}